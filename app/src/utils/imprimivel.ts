export interface Imprimivel {
  //Por padrão TODA interface é 'public'
  //Por padrão TODA interface precisa ter seus métodos implementados 'implements'
  //Por padrão TODO método de uma interface é 'abstract'
  paraTexto(): string;
}
